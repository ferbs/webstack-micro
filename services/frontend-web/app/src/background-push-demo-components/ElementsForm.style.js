import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles(theme => ({
  aiForm: {
    textAlign: 'center',
  },
  smartList: {
    width: '80%',
    margin: '1rem 2rem',
    padding: '.5rem',
  },
}));


export default useStyles;

