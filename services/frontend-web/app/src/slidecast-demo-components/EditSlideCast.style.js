import { makeStyles } from '@material-ui/styles';


const useStyles = makeStyles(theme => ({
  modalDialogPaper: {
    height: '80vh',
  },
  editMarkdown: {
    marginTop: '5rem',
  },
}));

export default useStyles;
